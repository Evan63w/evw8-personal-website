# Evan Waite's Personal Website 
My own website - in progress!

## GitHub Pages Deployment

## Local Development

To run the portal locally:

```bash
# Using Python 3
python3 -m http.server 8000

# Then open http://localhost:8000 in your browser
```

Or simply open `index.html` in your browser (though a local server is recommended to avoid CORS issues).

## Project Structure

```
react-portal/
├── index.html      # Main HTML file
├── app.js          # React components and logic
├── styles.css      # Styling and animations
└── README.md       # This file
```

## Technologies Used

- React 18 (via CDN)
- Vanilla CSS with modern features
- No build process required
- No dependencies to install

## TODO
- Implement Node.js when adding connection to AWS
- Add resume
